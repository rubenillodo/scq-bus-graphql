# import DateTime from '../DateTime/schema.graphql'

type Stop {
  id: ID!
  name: String!
  areaName: String
  location: Location!
  arriving: [Arriving!]!
}

type Arriving {
  line: Line!
  arriveAt: DateTime!
}

type Location {
  latitude: Float!
  longitude: Float!
}
